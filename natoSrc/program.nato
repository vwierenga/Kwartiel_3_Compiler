this is program over

verify 1 < 2 OR 2 !< 3 over
    say true over
endVerify over

execute all before 1 < 2 over
    falcon testy <- 1 - 2 over
    say testy over
endExecute over

operation mango falcon variablenameone message twelve over
    say nothing over
    payload <- TRUE over
endOperation over

start operation mango over
falcon test start operation mango over

over and out
